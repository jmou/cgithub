{
  "name": "cgithub",
  "version": "1.0.0",
  "description": "Lightweight GitHub alternative frontend",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "node --watch --watch-preserve-output src/index.ts",
    "build": "rm -rf dist/* && esbuild --bundle --format=esm --platform=node --outdir=dist/build --sourcemap src/index.ts && cp -R static views dist/",
    "test": "node --test src/scraper.test.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "eta": "^3.5.0",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.27.2",
    "prettier": "^3.8.1",
    "typescript": "^5.7.2"
  }
}
